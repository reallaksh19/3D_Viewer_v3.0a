# BM_CII Latest GLB Mock Path

The **Basic GLB-PCF → Quick Mocks → BM_CII** button reads:

```text
benchmarks/bm-cii/latest-glb-manifest.json
```

The default target GLB is:

```text
benchmarks/bm-cii/latest/BM_CII_latest_engineering.glb
```

For future generated BM_CII review GLBs, either:

1. Replace `BM_CII_latest_engineering.glb` at this path, or
2. Update `../latest-glb-manifest.json` to point to the generated GLB path.

Keep this mock GLB as the latest accepted BM_CII visual benchmark artifact.
